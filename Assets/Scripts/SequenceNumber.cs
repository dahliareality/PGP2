using UnityEngine;
using System.Collections;

// Part of the Sequence puzzle. This should be attached to each button that should be part of the script. Make sure that these buttons are tagged as "Sequence Puzzle".

public class SequenceNumber : MonoBehaviour {

    public int NumberForSequence;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
